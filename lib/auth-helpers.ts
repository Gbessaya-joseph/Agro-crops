// import { cookies } from 'next/headers';

// export async function getCsrfToken() {
//   const token = (await cookies()).get('next-auth.csrf-token')?.value.split('|')[0];
//   return token || '';
// }

// Vous pouvez ajouter d'autres fonctions liées à l'auth ici